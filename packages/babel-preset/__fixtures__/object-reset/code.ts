import { makeStyles } from '@griffel/react';

export const useStyles = makeStyles({
  root: { color: 'red', paddingLeft: null },
  button: { border: '3px solid red', background: 'green' },
  icon: { color: null, padding: '4px' },
});
