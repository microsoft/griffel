{
  "name": "@griffel/transform-shaker",
  "$schema": "../../node_modules/nx/schemas/project-schema.json",
  "sourceRoot": "packages/transform-shaker/src",
  "projectType": "library",
  "tags": [],
  "targets": {
    "lint": {
      "executor": "@nx/eslint:lint",
      "outputs": ["{options.outputFile}"]
    },
    "test": {
      "executor": "@nx/vitest:test",
      "outputs": ["{options.reportsDirectory}"],
      "options": {
        "reportsDirectory": "{projectRoot}/../../coverage/packages/transform-shaker"
      }
    },
    "build": {
      "executor": "nx:run-commands",
      "outputs": ["{workspaceRoot}/packages/transform-shaker/dist"],
      "options": {
        "cwd": "packages/transform-shaker",
        "commands": [
          "tsc --build tsconfig.lib.json --pretty",
          "node ../../tools/copy-pkg-assets.mjs packages/transform-shaker"
        ],
        "parallel": false
      }
    }
  }
}
