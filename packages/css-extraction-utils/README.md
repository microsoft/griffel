# Shared utils to perform CSS extraction in Griffel

Internal utils shared by Griffel bundler integrations ([`@griffel/webpack-plugin`](../webpack-plugin) &
[`@griffel/vite-plugin`](../vite-plugin)).

> ⚠️ This package is an implementation detail, there are no guarantees about its API. Use a bundler integration instead.

> ⚠️ This package is a scaffolding only, the implementation follows in a separate pull request.
