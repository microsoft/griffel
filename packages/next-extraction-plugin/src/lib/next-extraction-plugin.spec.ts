import { nextExtractionPlugin } from './next-extraction-plugin';

describe('nextExtractionPlugin', () => {
  it('should work', () => {
    expect(nextExtractionPlugin()).toEqual('next-extraction-plugin');
  });
});
