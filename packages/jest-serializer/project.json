{
  "name": "@griffel/jest-serializer",
  "$schema": "../../node_modules/nx/schemas/project-schema.json",
  "sourceRoot": "packages/jest-serializer/src",
  "projectType": "library",
  "tags": [],
  "targets": {
    "lint": {
      "executor": "@nx/eslint:lint",
      "outputs": ["{options.outputFile}"]
    },
    "test": {
      "executor": "@nx/vitest:test",
      "outputs": ["{options.reportsDirectory}"],
      "options": {
        "reportsDirectory": "{projectRoot}/../../coverage/packages/jest-serializer"
      }
    },
    "build": {
      "executor": "nx:run-commands",
      "outputs": ["{workspaceRoot}/packages/jest-serializer/dist"],
      "options": {
        "cwd": "packages/jest-serializer",
        "commands": [
          "tsc --build tsconfig.build.json --pretty",
          "node ../../tools/copy-pkg-assets.mjs packages/jest-serializer"
        ],
        "parallel": false
      }
    }
  }
}
