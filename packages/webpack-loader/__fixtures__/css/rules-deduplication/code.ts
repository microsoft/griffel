import { makeStyles } from '@griffel/react';

export const styles1 = makeStyles({
  root: {
    color: 'red',
    backgroundColor: 'green',
  },
});

export const styles2 = makeStyles({
  root: {
    color: 'red',
    backgroundColor: 'green',
  },
});
